spring:
  profiles:
    active: dev

server:
  servlet:
    encoding:
      charset: UTF-8
      force: true
spotify:
  api-url: https://api.spotify.com

jdbc:
  driver-class-name: org.h2.Driver
  url: jdbc:h2:mem:test
  user: someuser
  pass: somepassword